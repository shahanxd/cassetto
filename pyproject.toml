[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cassetto"
version = "1.0.0"
description = "Give your LLM structural awareness of any codebase via MCP"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [{name = "Shahan"}]
keywords = ["mcp", "code-intelligence", "tree-sitter", "semantic-search", "call-graph", "claude"]

dependencies = [
    "fastmcp",
    "lancedb",
    "requests",
    "pyarrow",
    "tree-sitter",
    "tree-sitter-language-pack",
    "duckdb",
    "networkx",
    "scipy",
    "watchdog",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-timeout"]

[project.urls]
Homepage = "https://github.com/shahanxd/cassetto"
Repository = "https://github.com/shahanxd/cassetto"

[project.scripts]
cassetto = "cassetto.indexer:main"

[tool.setuptools]
packages = ["cassetto"]
